*** Variables ***
${header.title.toolbar}    //div[@id='dashboard-toolbar']    # Verify user navigated to correct page or not.
${header.confirmation.message}    //div[text()='Confirm']
${header.feature.table}    //thead[@class='full-width']    # Shows the header of a table.
${header.tableactions.message}    //div[text()='Move to the bin']
